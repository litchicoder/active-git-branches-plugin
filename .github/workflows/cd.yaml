# Auto-release is disabled until jenkinsci hosting + RPU CD secrets exist.
# Re-add push/check_run triggers after MAVEN_USERNAME and MAVEN_TOKEN appear.
name: CD
on:
  workflow_dispatch:

jobs:
  cd:
    uses: jenkins-infra/github-reusable-workflows/.github/workflows/maven-cd.yml@v1
    secrets: inherit
